<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.moha.demo.dao.AsUserMapper" >
  <resultMap id="BaseResultMap" type="com.moha.demo.entity.AsUser" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Mon Feb 05 14:41:23 CST 2018.
    -->
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="userCode" property="userCode" jdbcType="VARCHAR" />
    <result column="userName" property="userName" jdbcType="VARCHAR" />
    <result column="userPassword" property="userPassword" jdbcType="VARCHAR" />
    <result column="creationTime" property="creationTime" jdbcType="TIMESTAMP" />
    <result column="lastLoginTime" property="lastLoginTime" jdbcType="TIMESTAMP" />
    <result column="createdBy" property="createdBy" jdbcType="VARCHAR" />
    <result column="lastUpdateTime" property="lastUpdateTime" jdbcType="TIMESTAMP" />
    <result column="isStart" property="isStart" jdbcType="INTEGER" />
    <result column="roleid" property="roleid" jdbcType="INTEGER" />
    <result column="salt" property="salt" jdbcType="VARCHAR" />
  </resultMap>
  <sql id="Base_Column_List" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Mon Feb 05 14:41:23 CST 2018.
    -->
    id, userCode, userName, userPassword, creationTime, lastLoginTime, createdBy, lastUpdateTime, 
    isStart, roleid, salt
  </sql>
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Mon Feb 05 14:41:23 CST 2018.
    -->
    select 
    <include refid="Base_Column_List" />
    from as_user
    where id = #{id,jdbcType=INTEGER}
  </select>

  <!-- 显示所有用户 -->
  <resultMap type="com.moha.demo.entity.AsUser" id="userBean">
    <id column="id" property="id" />
    <result column="createdBy" property="createdBy" />
    <result column="creationTime" property="creationTime" />
    <result column="isStart" property="isStart" />
    <result column="lastLoginTime" property="lastLoginTime" />
    <result column="lastUpdateTime" property="lastUpdateTime" />
    <result column="userCode" property="userCode" />
    <result column="userName" property="userName" />
    <result column="userPassword" property="userPassword" />
    <!-- 多对一的关系 -->
    <!-- property: 指的是属性的值, javaType：指的是属性的类型 -->
    <association property="role" javaType="com.moha.demo.entity.AsRole">
      <id column="id" property="id" />
      <result column="createdBy" property="createdBy" />
      <result column="creationTime" property="creationTime" />
      <result column="isStart" property="isStart" />
      <result column="lastUpdateTime" property="lastUpdateTime" />
      <result column="roleName" property="roleName" />
    </association>
  </resultMap>
  <select id="userlist" resultMap="userBean">
    select as_user.*,as_role.rolename from
    as_user inner join as_role on as_role.id = as_user.roleid where 1=1
    <if test="userName!=null">
      and as_user.userName like #{userName}
    </if>
    <if test="roleid!=0">
      and as_user.roleid =#{roleid}
    </if>
    <if test="isStart!=2">
      and as_user.isStart =#{isStart}
    </if>
    LIMIT #{lineNum},#{pageSize}
  </select>
  <select id="num" resultType="int">
    select count(1) from(select as_user.*,as_role.rolename from
    as_user inner join as_role on as_role.id = as_user.roleid where 1=1
    <if test="userName!=null">
      and as_user.userName like #{userName}
    </if>
    <if test="roleid!=0">
      and as_user.roleid =#{roleid}
    </if>
    <if test="isStart!=2">
      and as_user.isStart =#{isStart}
    </if>
    ) a
  </select>
  <!-- 增加用户 -->
  <insert id="addUser" parameterType="com.moha.demo.entity.AsUser" useGeneratedKeys="true" keyProperty="id">
    insert into
    as_user(userCode,userName,userPassword,creationTime,lastLoginTime,createdBy,lastUpdateTime,isStart,roleid,salt)
    values(#{userCode},#{userName},#{userPassword},#{creationTime},#{lastLoginTime},#{createdBy},#{lastUpdateTime},#{isStart},#{role.id},#{salt})
  </insert>

  <!-- 修改用户 -->
  <update id="modifyUser" parameterType="com.moha.demo.entity.AsUser">
    update as_user set salt=#{salt},userCode=#{userCode},userName=#{userName},userPassword=#{userPassword},roleid=#{role.id},isStart=#{isStart},lastUpdateTime=#{lastUpdateTime} where id=#{id}
  </update>
  <!-- 删除用户-->
  <delete id="delUser" parameterType="int">
    delete as_user where id=#{id}
  </delete>
  <!-- 查询用户-->
  <select id="user" parameterType="int" resultType="com.moha.demo.entity.AsUser">
    select * from as_user where id=#{id}
  </select>

  <select id="findUsers" resultType="com.moha.demo.entity.AsUser">
    select * from as_user where 1=1
    <if test="userName!=null">
      and userName like #{userName}
    </if>
    <if test="roleid!=null">
      and roleid =#{roleid}
    </if>
    <if test="isStart!=null">
      and isStart =#{isStart}
    </if>
  </select>

  <select id="login" parameterType="string" resultType="com.moha.demo.entity.AsUser">
    select * from as_user where userCode = #{usercode,jdbcType=VARCHAR} and userPassword = #{password,jdbcType=VARCHAR}
  </select>

  <update id="pModify" >
    update as_user set userPassword = #{newpwd},salt=#{salt} where id = #{id}
  </update>

  <update id="lastLogin">
    update as_user set lastLoginTime = Sysdate() where id = #{id}
  </update>

  <!-- 代理商余额 -->
  <select id="balance" resultType="com.moha.demo.model.BalanceView">
    select
    u.id as userid,
    u.userName as userName,
    a.id as aid,
    a.money as money
    from as_user u
    inner join as_account a on u.id = a.userId
  </select>

  <select id="selectUserByusercode" parameterType="string" resultType="com.moha.demo.entity.AsUser">
    select * from as_user where userCode = #{usercode,jdbcType=VARCHAR}
  </select>
  <resultMap type="com.moha.demo.entity.AsUser" id="balanceMap">
    <id property="id" column="userid" />
    <result property="userName" column="userName"/>
    <association property="account" column="userId" javaType="com.moha.demo.entity.AsAccount" >
      <id property="id" column="aid" />
      <result property="money" column="money"/>
    </association>
  </resultMap>
</mapper>